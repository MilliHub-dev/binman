# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

This project runs on a development build, not Expo Go — see the `development` profile
in eas.json. Build with `npx eas-cli build --profile development --platform android`.
